import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        maroon: '#921A40',
        pink: '#C75B7A',
        peach: '#D9ABAB',
        lightPeach: '#F4D9D0',
        background: {
            default: '#F4D9D0',
        }
    },
});

export default theme;